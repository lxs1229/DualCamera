# DualCamera

面向 iPhone 16 Pro Max（iOS 26）的双摄像头拍摄示例。支持同时调用前后摄像头拍照，并合成一张画中画效果的图片，方便直接在 Xcode 中调试。

## 打开方式
1. `git clone` 到本地后，用 Xcode 打开 `DualCamera.xcodeproj`。
2. 运行前在真机上授予相机权限（需要支持多摄像头）。

## 说明
- 主画面使用后置广角摄像头，右下角为前置摄像头预览。
- 点击拍照按钮会同时抓取两张照片并合成一张画中画图片。
